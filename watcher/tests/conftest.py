"""
Pytest configuration for the watcher test suite.

sys.path is configured via pyproject.toml [tool.pytest.ini_options] pythonpath,
so no manual path manipulation is needed here.  This file exists as a home for
any shared fixtures that future tests may require.
"""
